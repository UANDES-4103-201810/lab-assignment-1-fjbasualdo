# lab-assignment-1
Base project for lab assignment 1

3. By activating the Element tab, it´s possible to see the page´s code and its structure. 
The <html> tag is thge container of all the other elements.
The <head> element in this case includes the title and the style of the website. 
The <body> tag defines the document´s body. It contains the main part of the webpage, the table itself. It is divided in 3 parts: 3 <tr>, which 
defines a row in a <table>.
The <tbody> is used to group the body content. It must be used as a child of a <table>.
The <center> tag is used to align text.

4. By activating the Source tab, it is possible to see 6 items:
(index), which contains the content of the table that was described on question 3, such as tags and the structure of the webpage.
https://news.ycombinator.com/hn.js?XmeMkOgGRBw9auMJ1Y7Q, has the functions of the items of the table.
https://news.ycombinator.com/news.css?XmeMkOgGRBw9auMJ1Y7Q, has the styleship of the table and the webpage. Here are defined the colours, fonts, etc.
Then, it is possible to see 3 .gifs, which we can imagine, these are kind of animations that are on the webpage.

5. The XMLHttpRequest object is a developers dream, because you can:

Update a web page without reloading the page
Request data from a server - after the page has loaded
Receive data from a server  - after the page has loaded
Send data to a server - in the background

6. The connection to this site is using a valid, trusted server certificate issued by COMODO RSA Domain Validation Secure Server CA.